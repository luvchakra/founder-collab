/** The /platform admin surfaces use their own dark zinc palette; the shared Export button
 * keeps its shape and behaviour there and only takes on that palette. */
export const PLATFORM_EXPORT_BUTTON_CLASS = "border-zinc-700 bg-zinc-900 text-zinc-100 hover:bg-zinc-800 hover:text-zinc-50";
