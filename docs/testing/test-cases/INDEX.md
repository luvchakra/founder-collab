# Test Coverage Index

Single source of truth for coverage state, across both automated scripts and
documented cases. Update this table in the same commit as any change to a test-case
file or `scripts/test-*.mjs`. See `TESTING_STRATEGY.md` for the policy.

**First execution pass:** 2026-09-08, see `../EXECUTION-2026-09-08.md` for what was
actually run/traced against `main` and what it found (four real gaps, none caught by
the existing automated suite: ADR-9's grace-period expiry is never invoked by anything,
`fsm`'s `cancelJob()` leaks reserved inventory stock forever, GST GSP credentials are
stored in plaintext rather than encrypted like BYOK keys, and `requireModule()` — named
by `CLAUDE.md` as a required enforcement layer — doesn't exist in the codebase).

Status values: `Automated` (real script/test in CI path) · `Documented` (manual case
file exists) · `Both` · `Partial` · `Not started`.

| Module | Test-case file | Automated coverage (existing) | Status | Notes |
|---|---|---|---|---|
| `core` | [core.md](test-cases/core.md) | `test-core-{parties,addresses,items,number-sequences,documents,payments,tags-fields-attachments,domain-events,audit-log,api-keys,messages}-rls.mjs` | Both | RLS layer thorough; license-lifecycle workflow (§5 of strategy doc) still only documented. A later UX pass added TC-CORE-015/016: `createParty()`'s new optional admin-client override (RLS-bypass path for CRM's webhook ingestion — a real, narrow security-model change worth watching) and the new `core.businesses.disabled_at` business-disable feature. TC-CORE-013/014 updated for the API-keys relocation (now business-wide, covering fsm/crm/gst resources too via a composite dispatcher) and `core.messages.channel`'s extended constraint (CRM's 4 new channel values) |
| `discovery` | [discovery.md](test-cases/discovery.md) | `test-discovery-rls.mjs`, `test-discovery-party-backfill.mjs`, `is-provider-failure.test.ts` | Both | Feature/AI-pipeline behavior documented only; consider porting `co-founder-ai`'s `score-prospect`/`normalize-url` unit tests into this package. **TC-DISCOVERY-004 corrected this pass — a real, deliberate reversal, not a bug:** BYOK no longer strictly forbids any company-owned fallback; an account with no BYOK connected now falls back to an optional `PLATFORM_AI_API_KEY`, with `credentialSource` telling callers which happened. Added TC-DISCOVERY-013..016 for the AI-driven product-catalog auto-populate, the delete-product/delete-knowledge-source fixes, the compact-card prospect layout, and discovery's first `contract/index.ts` (the CRM handoff) |
| `inventory` | [inventory.md](test-cases/inventory.md) | `test-inventory-{rls,procedural,compat-views}.mjs`, `test-sales-returns-workflow.mjs` | Both | Best-covered module at the automated-workflow level; extend that pattern to PO/SO/invoice chains. TC-INVENTORY-002's open question (is the insufficient-stock error message UI-clear, not just a DB constraint) is now closed — traced end to end and confirmed already good, no fix needed. A later UX pass added TC-INVENTORY-014..016: a real dashboard-crash fix (selecting nonexistent columns — also silently broke the AI chat's "consult inventory" path, same root cause), and the new `listRecentOrdersForParty`/`getAlerts` contract functions feeding the cross-module Customer 360 panel and the now-platform-wide notification bell |
| `fsm` | [fsm.md](test-cases/fsm.md) | `test-fsm-rls.mjs`, `test-fsm-workflow.mjs` | Both | Workflow script covers the opportunity→job→invoice chain, tenant isolation, and `core.has_permission()` data for the `opportunities.edit`/`estimates.edit`/`jobs.edit`/`jobs.reopen` keys added this pass (TC-FSM-015..018) — fixed a real gap where those permissions were declared in migrations but never enforced anywhere in application code. Still a DB-level harness only: the actual `requirePermission()` TS call sites and job status-transition guards aren't exercised by an automated test yet (see the script's own header comment) — that needs a TS-level harness this repo doesn't have. `schedule.manage`/`schedule.print_work_orders`/`time_entries.edit`/`expenses.edit`/`notes.edit`/`messages.manage` permission enforcement was out of scope for this pass and remains unverified — same likely gap, not yet audited. Added TC-FSM-019/020 for the new `getAlerts`/`listRecentJobsForParty` contract functions |
| `crm` | [crm.md](test-cases/crm.md) | `test-crm-rls.mjs`, `evaluate.test.ts` (18 cases), `verify-meta-signature.test.ts` (11 cases) | Both | **Grew past skeleton status this pass** implementing `docs/design/crm-module-design.md`'s P0/P1 scope: channel accounts + real Meta/WhatsApp inbound webhooks with genuine HMAC signature verification, instant-reply, discovery's first contract-based handoff ("Convert to prospect"), the Customer 360 panel, ticket enrichment, and two real routing-rule conditions (known-sender, business hours — TC-CRM-001 corrected from "no routing engine at all" to describe exactly what's evaluated and what still isn't, e.g. AI-detected intent, schema-only). TC-CRM-003 promoted P2→P0 now that inbound messages genuinely round-trip through `core.threads/messages`. Google Business Messages and AI-drafted replies explicitly deferred, documented in both the design doc and TC-CRM-001/007/011 rather than silently skipped. Still only a DB/RLS-harness — none of the new webhook/AI/cross-module behavior has automated coverage yet (TC-CRM-006..011 all flag this same gap) |
| `gst` | [gst.md](test-cases/gst.md) | `test-gst-credentials-rls.mjs`, `test-gst-generation-history-rls.mjs`, `gsp-client.test.ts` | Both | RLS/license/permission gating already thorough at the DB level. This pass added the platform's first genuinely TS-level (not raw-SQL) automated test — `gsp-client.test.ts`'s mocked-`fetch` coverage for `callGsp`'s sanitized error messages (TC-GST-007), fixing a real bug where a GSP failure leaked its internal request URL straight into the UI. Also corrected TC-GST-002 (generation *failures* leave zero trace in `gst.einvoices`/`eway_bills` today — no `'failed'` status exists in the schema — contrary to what that case previously claimed) and TC-GST-006 (module now has 5 nav items under "Compliance", not 4). A later UX pass added TC-GST-008/009 for the breadcrumb fix (matching inventory/fsm's generic pattern) and the new `getAlerts` contract function |
| Platform shell (non-module) | [platform-shell.md](test-cases/platform-shell.md) | `scripts/lint-import-boundaries.mjs` (+ its own test), `perf-check-tenant-license-rls.mjs` | Partial | UI/UX area — hold off on e2e until shell stops churning (see strategy §5). TC-SHELL-002's "CONFIRMED FAILING" status was stale (fixed by task #25 two platform sessions ago, never updated here) — corrected to FIXED this pass, re-verified by reading the current `(dashboard)/layout.tsx`. Also fixed a real gap (TC-SHELL-010): `(auth)`, `onboarding`, and the public `p/**` routes plus the root layout itself had no `error.tsx`/`global-error.tsx` at all — any error there fell through to Next.js's bare default page. Added a shared `packages/core/src/components/errors/error-notice.tsx` and wired up all four missing boundaries. A later UX pass added TC-SHELL-011..016: the module-picker's lock-icon fix (licensing only, never the pin state), a cosmetic business-header cleanup, the Admin & settings hub's new Business section (disable/enable + business-wide API keys), the Control-Center→Executive-Dashboard rename with section descriptions, its new PDF-export button, and the AI-credits sidebar indicator now hidden once BYOK is connected. TC-SHELL-005 corrected: the alert bell now aggregates every licensed module's own alerts, not just discovery's |
| Menu / navigation smoke (all modules) | [menu-smoke.md](test-cases/menu-smoke.md) | `apps/web/tests/menu-routes.test.ts` | Both | **0 currently-failing items** — the 3 this file used to list as confirmed failing (2 `fsm` missing routes, 1 sidebar licensing-filter gap) were all fixed by tasks #25/#26, but this doc was never updated to say so until this pass; also corrected a related overclaim that direct-URL access to an unlicensed module went completely unblocked — the route guard is real, it just lives in `packages/core/src/db/middleware.ts#updateSession` rather than in `proxy.ts`'s own body, which is what the original finding actually read. Also fixed `apps/web/proxy.ts`'s own stale comment describing this gating as unbuilt future work. A later UX pass updated TC-MENU-DISC-001 for discovery's new Overview/Products two-group sidebar, added TC-MENU-CRM-004/005 for the new channel-accounts panel and the dynamic Customer 360 route, and added TC-MENU-ACCT-008 explicitly disambiguating the module switcher's own "Admin" shortcut (`/dashboard/settings`) from the account menu's differently-gated "Admin" item (`/dashboard/admin`) |
| Cross-cutting architecture rules | — | `lint-import-boundaries.mjs`, `lint-migration-schema.mjs` | Automated | Enforced in the `npm test`/CI path already; not duplicated here |

## How to add a new row

New module moves past skeleton, or a new cross-cutting concern emerges → add a row
here in the same PR that adds or meaningfully extends its test-case file.
